if (global.playerCarrying == false && global.playerCarrying == false) {
	with (obj_player) {
		// drop move speed
	}
	global.foodCarrying = true;
	global.playerCarrying = true;
	instance_destroy(self);
}